torch>=2.4
transformers>=4.51
trl>=0.16
peft>=0.14
vllm>=0.8
datasets
accelerate
math-verify
tilelang                 # GPU kernel compiler — dependency for FlashQLA (Qwen3.5 GDN layers)
flash-linear-attention   # FlashQLA kernels for Qwen3.5 hybrid GDN/linear attention layers
tensorboard
hf_transfer
liger-kernel>=0.5    # Fused CE loss — critical for 248K vocab (avoids 30GB logits)
pyyaml
numpy
pandas
matplotlib
tqdm
