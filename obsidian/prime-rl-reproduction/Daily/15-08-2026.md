---
type: daily
date: 15-08-2026
project: prime-rl-reproduction
---
# Daily Note — 15-08-2026

## Vanilla GRPO scheduler ablation

- Завершённый cosine baseline имеет 187 steps, `lr=1.5e-6`, `5%` warmup и
  decay почти до нуля; frozen-val single-generation accuracy колебалась без
  устойчивого улучшения (`.68` start, `.65` final).
- Подготовлен single-variable constant profile: тот же LR и весь остальной
  contract, но `lr_scheduler_type=constant`, `warmup_ratio=0`.
- Contract tests на H200: `13 passed`.
- Full run запущен в `11:38 MSK` на physical GPU 1:
  `grpo_vanilla_constant_val20_g1_r1`; output
  `outputs/grpo_vanilla_constant_full_gpu1_val20_postsync_r1`.
- Перед содержательной интерпретацией нужны clean finish, raw metrics и fresh
  frozen-val `maj@8`; validation `G=1` остаётся только trajectory diagnostic.

See [[../Experiments/GRPO-DPO-Z-GPU7]].

## Constant GRPO learning-rate ablation

- Параллельно запущен `lr=5e-6` при том же constant scheduler и неизменном
  actor/data/reward/eval contract; tests: `14 passed`.
- tmux `grpo_vanilla_constant_lr5e6_val20_g4_r1`, physical GPU 4; output
  `outputs/grpo_vanilla_constant_lr5e6_full_gpu4_val20_postsync_r1`.
- GPU 4 уже была compute-busy у другого пользователя: это throughput confound,
  но не запланированное изменение algorithmic contract. Сравнивать будем
  качество, не runtime.

## GRPO health checkpoint — 12:32 MSK

- Constant `1.5e-6`: step `18/187`; healthy sync (`changed=true`,
  `max_diff=0`), no OOM/NaN.
- Constant `5e-6`: step `9/187`; healthy sync, no OOM/NaN.
- DPO-Z cosine: step `129/187`; latest frozen val at step 120 accuracy `.65`.
- Обе constant branches имеют одинаковую start val: accuracy `.68`, mean
  length `6004`, clipped `.14`. Следующая содержательная точка — paired val@20.

## GRPO checkpoint — 18:05 MSK

- DPO-Z retry4 cleanly finished `187/187` за `17:59:53`; final adapter и
  checkpoints 150/187 сохранены. Final frozen `G=1` accuracy `.65` против
  start `.68`; финальный `maj@8` ещё не измерен.
- Constant `1.5e-6` жив на step `142/187`, sync healthy, no OOM/NaN. Val@140
  accuracy `.60`; trajectory остаётся noisy и ниже start `.68`.
- Constant `5e-6` crashed после step 44: OOM при vLLM KV-cache wake-up на
  shared GPU 4, не в policy loss. Checkpoint отсутствует из-за `save_steps=75`,
  поэтому resume невозможен. Partial val: `.68` at step20, `.69` at step40;
  это лишь основание для clean rerun, не результат.
