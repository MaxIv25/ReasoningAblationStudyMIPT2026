# Exp 5.3b: DAPO on SFT model with CALIBRATED data
# Purpose: Fair comparison with PRIME on the same calibrated dataset
# Key: isolates method difference (dense vs sequence rewards) from data effect
run_name: "exp5_3b_dapo_calibrated"

model:
  name: "outputs/exp2_1_full_ft"

grpo:
  loss_type: "dapo"
  epsilon: 0.2
  epsilon_high: 0.28    # Asymmetric: encourage exploration
